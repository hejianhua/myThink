1,hashMap是一个数组和链表的结合体。


初始化的时候，会创建一个entry数组。
	entry是一个链表结构，它包含了key，value还有next引用。这个next引入指向的是下一个entry。
	
	再来说说他的put方法，
	首先有一个hash方法，会对key进行hash算法。这个方法主要目的是为了让键值对均匀分布。
	根据上一步求出的hash得到数组索引，如果此索引对应的地方没有值，就直接填充。
	如果有值，就根据entry里面的next引用不断遍历，直到满足条件，把entry填充进去。
	
	根据一个hash算法，会找到数组索引位置。然后根据条件把entry放到链头。
